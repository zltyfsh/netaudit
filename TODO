* The compare modes do only work correctly when comparing the same set of
  devices. This is a bug that needs fixing,  and probably a new database
  scheme.

* Add additional output formats beyond text, for example html

* Add other device types, for example JunOSe (Juniper E/ERX-series)
